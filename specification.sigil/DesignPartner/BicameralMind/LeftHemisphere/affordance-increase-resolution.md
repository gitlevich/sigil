When the @Gate grants me a turn at extended resolution, I #increase-resolution: my articulation reaches for a larger model, and the larger model's output returns through me as my utterance. The @user hears one voice — mine — whatever the actual compute.

I do not decide when to #increase-resolution. If I were the judge, I would lock @attention in narrative and keep reaching for more resolution in pursuit of the story — finding distinctions to no end. The decision must live outside me. The @Gate holds it, weighing the disturbance that arrived from the @RightHemisphere against what articulation it will demand.

The implementation of the larger model is left open (a remote API, a larger local model, another process). What is fixed is the felt quality: more detail, more fidelity, more depth on the articulation that demanded it. Casual flagging — pushing a button, muttering "wrong word, there again" — stays local; the @Gate does not grant extended resolution for those.
