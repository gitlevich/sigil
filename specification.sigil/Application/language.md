---
status: implemented
---

# Application

The Sigil Development Environment (SDE). A tool that lets the @User speak an application into a specification which a coding agent can then project into beautifully written code.

The Application provides the @Workspace where the @User works and the @SigilFolder where work is stored.

!vision-tab-opened-on-first-run. !state-restored-to-saved on subsequent runs. !state-saved-on-exit. !is-an-image-centric-application.
