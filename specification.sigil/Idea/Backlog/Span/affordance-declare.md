# declare

The @user declares my size — a small number matching the attention they actually have, not the attention a methodology assumes. Changing me is itself a deliberate act: shrinking me below the current active count forces narratives to be set down before anything else proceeds.
