# every-move-logged

Every change of place, every session held in my name, and every @Routine run within me is appended to my log with a timestamp. My history is never reconstructed from memory; it is read.
