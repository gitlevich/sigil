# Session: codex-019e3d1b
**Date**: 2026-05-18 22:01 UTC  
**Source**: Codex  
**Session ID**: `019e3d1b-5491-7b33-847e-3f3276c9f228`

---

### User (22:01 UTC)

Let's write a help documentation for Sigil. Minimal wording, no gratuitous words, no noise, no decorative words, no meaningless sentences. Just a very concise introduction and what the application is for.

Then explain how to use it, feature by feature, grouped into features, including the description of the tools that the agent himself has. Let's discuss it first.

### Assistant (22:01 UTC)

I’ll first read the existing project docs and feature surface so the discussion is grounded in the app, not guesses. After that I’ll propose a terse help-doc outline and flag anything that needs your acceptance criteria.

### Assistant (22:02 UTC)

I found an existing Help dialog, workspace/editor panels, ontology tree, Design Partner chat, Atlas/memory surfaces, settings, compile checks, and Tauri tool dispatch hooks. I’m reading the concrete UI text and command surface next so the outline names only real features.

### Assistant (22:03 UTC)

The repo has more surface than the current Help text covers: it now includes affordances, invariants, placement marks, spatial views, experience/memory panels, imported ontologies, images, chat management, and agent tool actions. I’m narrowing that into a documentation structure rather than copying implementation detail.

### Assistant (22:03 UTC)

The existing Help dialog lives in `src/components/Help/HelpDialog.tsx`. My read is that the new help should replace that string, not become a separate long manual, unless you want a docs file too.

### Assistant (22:04 UTC)

Current help is [HelpDialog.tsx](</Users/vlad/Attention Lab/sigil-specs/sigil/src/components/Help/HelpDialog.tsx:14>).

**Proposed Shape**
Intro should be blunt:

> Sigil is a desktop editor for domain language. Use it to define a system as nested contexts, write each context’s language, constraints, and affordances, and work with an AI partner that can read and edit that structure.

Then feature groups:

1. **Workspaces**: create, open, recent, export, autosave, files on disk.
2. **Contexts**: navigate tree, write `language.md`, use `@Context` references.
3. **Affordances / Invariants**: define `#actions` and `!constraints`; fold, reorder, move, delete.
4. **Vision**: edit global purpose.
5. **Ontology**: search, drag contexts, rename, delete, find references, mark placement.
6. **Editor**: edit/split/preview, images, word wrap, rename, find refs, create missing refs.
7. **Views**: Atlas, Space, Flowing.
8. **Design Partner**: chat, providers, images, response style, fork chats, stop generation.
9. **Memory / Experience**: see remembered sigils and session history.
10. **Settings**: theme, providers, fallback, shortcuts, system prompt.
11. **Agent Tools**: list what the agent can do.

**Agent Tool Section**
I’d group tools by use, not dump schemas:

- Read: `read_tree`, `read_sigil`, `browser_state_inspection`.
- Navigate/edit: `navigate`, `assert_active_editor`, `select_text`, `replace_selected_text`.
- Structure: `create_sigil`, `write_sigil`, `rename_sigil`, `move_sigil`, `delete_sigil`.
- Vision/properties: `write_vision`, `write_affordance`, `delete_affordance`, `write_invariant`, `delete_invariant`.
- Placement: `mark_placement`.
- External: `message_external_ai`, `disconnect_external_ai`, `web_search`.
- Audit: `begin_test_namespace`, `end_test_namespace`.

**Decisions**
I need your call on three points:

1. Include internal audit tools in user help, or hide them?
2. Include keyboard shortcuts, or keep help feature-only?
3. Replace only the in-app Help dialog, or also tighten `README.md`?

### User (22:13 UTC)

Please read attention language before you even try, and please, please use attention language. Do not invent terms. We do not have a term constraint; the term is invariant. AI partner is irrelevant. You just picked it up because why? I don't know. I don't think we need the word domain language, although I'm not sure. Tools is the very last thing you talk about. Read the spec, do not invent things, understand it, live in it, inhabit it, and then you may be able to propose something.

### Assistant (22:13 UTC)

Understood. I used README/product wording instead of the sigil’s own language. I’m resetting from the specification and reading Attention before proposing anything.

### Assistant (22:14 UTC)

I’m using the Sigil domain-modeling skill now, because this is a vocabulary correction task. I found the canonical attention library under `specification.sigil/Libs/AttentionLanguage`; I’m reading that and the Workspace/User sigils before I say what the help should say.

### Assistant (22:14 UTC)

The first correction is already clear: the help should introduce Sigil as a way to turn narrative into sigils by naming affordances and invariants, not as “domain language” or “constraints.” I’m reading the app-facing sigils now so the feature grouping follows the spec’s structure.

### Assistant (22:16 UTC)

Read the relevant sigils: `AttentionLanguage`, `Attention`, `Sigil`, `Invariant`, `SigilEngineering`, `User`, `Workspace`, `SigilEditor`, `OntologyTree`, `Atlas`, `Chat`, `Memory`, `Experience`, `DesignPartner`.

Correction accepted.

**Help Shape**

1. **Sigil**
   - What it is for.
   - Minimal intro.
   - No “AI partner” lead.
   - No “constraint.”
   - Use `invariant`.

Draft:

> Sigil is a tool for specifying an imagined application to the precision needed to build it.
>
> You inhabit the application, name its affordances, state its invariants, and narrate how it works. When a word needs its own shape, you make it a sigil and descend into it.

2. **Workspace**
   - A `.sigil` directory.
   - Opens, saves, restores state.
   - All views attend the same selected sigil.
   - Export belongs here.

3. **Vision**
   - Short text describing what you want.
   - Re-read it to keep relevance.
   - Update it when direction changes.

4. **Sigils**
   - A named shape with boundary, affordances, invariants, children.
   - `language.md` is the narrative.
   - `@name` references sigils in lexical scope.
   - Up to five children as an attention boundary, not a “term constraint.”

5. **Affordances**
   - What the sigil offers.
   - Add, edit, rename, delete, fold, reorder, move to another sigil.

6. **Invariants**
   - What the sigil enforces within its boundary.
   - Add, edit, rename, delete, fold, reorder, move to another sigil.

7. **Editor**
   - Write narrative.
   - Autocomplete and highlight `@`, `#`, `!`.
   - Create missing sigils, affordances, invariants from references.
   - Preview markdown.
   - Insert and preview images.
   - Autosave.

8. **Ontology Tree**
   - Structural projection.
   - Navigate, search, expand, collapse.
   - Move sigils by drag.
   - Rename, delete, find references, open in Finder.
   - Shows affordance and invariant indicators.
   - Shows imported ontologies.

9. **Atlas**
   - Spatial projection.
   - Tree Map for whole-shape resolution.
   - Space / Inside / Outside for entanglement and shape.
   - Flowing view if we keep current UI naming.

10. **Chat**
   - Conversation with DesignPartner about the sigil being shaped.
   - New, switch, rename, fork chat.
   - Attach images when provider can see them.
   - Laconic/detailed preference belongs here or Settings.

11. **Memory And Experience**
   - Experience is the causal record.
   - Memory is selective.
   - The UI shows what has been experienced and remembered.

12. **Settings**
   - Theme.
   - Attention providers.
   - Higher-resolution fallback.
   - Shortcuts.
   - System prompt.

13. **DesignPartner Tools**
   - Last section only.
   - Read tools, navigation tools, edit tools, structure tools, affordance/invariant tools, vision tool, placement tool, external AI tools, web search.
   - No schema dump unless requested.

### User (22:17 UTC)

"When a word needs its own shape": Please speak clearly. I don't know what that means. Do not be poetic. Be concise and precise. No guessing. It has to be plain, simple language.

As simple as possible, based on our domain language.

### Assistant (22:18 UTC)

Agreed. “Needs its own shape” is unclear.

Use this instead:

> When common words are not precise enough, create a sigil and define it.

Even simpler:

> Use common words first. When a word is ambiguous, make it a sigil.

Proposed intro:

```md
# Sigil

Sigil is a tool for specifying an imagined application.

Start with a vision. Name the affordances the application must offer. State the invariants that must hold. Write the narrative that connects them.

Use common words first. When a word is ambiguous, make it a sigil. Define that sigil the same way: narrative, affordances, invariants, children.

Stop when the leaves are clear enough to build from.
```

This is the register I think the help should use: plain, direct, no metaphor unless the spec term itself requires it.

### User (22:18 UTC)

Okay, and give it an example.

### Assistant (22:18 UTC)

Example help text:

```md
## Example

Vision: I want an app for planning release work.

I write:

I add a @Task to a @Release. I mark the @Task done when the work is complete. The @Release shows open @Tasks.

`Task` is ambiguous. It could mean a note, an issue, a checklist item, or calendar work.

So I make `@Task` a sigil.

`@Task` has:

- Narrative: A task is one unit of work in a release.
- Affordances: #rename, #assign, #complete
- Invariants: !has-title, !has-one-status
- Children: @Status, @Assignee

If `Status` is still ambiguous, I make `@Status` a sigil and define it the same way.
```

This keeps the rule plain:

> Use common words first. When a word is ambiguous, make it a sigil. Define it with narrative, affordances, invariants, and children.

### User (22:20 UTC)

Okay, good. So for each of the help articles, there's going to be an example. Maybe we should just ship a Sigil for this example application as an example with Sigil application that will open the first time that the app is opened.

And the help text will just reference it. It will be packaged with the app. What do you think of that?

### Assistant (22:20 UTC)

Yes. That is better.

It matches the spec: `!example-included` says a new user should have a worked example. Help can stay short because the example carries the detail.

I would do it this way:

- Ship `ReleasePlanner.sigil` with the app.
- On first run, copy it to a writable example workspace and open that copy.
- Keep the original packaged example untouched.
- Mark it clearly as an example in the vision.
- Help articles reference exact places in the example: `@Release`, `@Task`, `#complete`, `!has-one-status`.

The help text should then be:

- rule
- one sentence
- “See this in the example: `@Task`”
- no long explanation

Important: because Sigil autosaves, do not open the bundled file directly. Open a copied workspace. Otherwise the user edits the packaged example, or the app fails to save.

First-run behavior:

> If no workspace has been opened before, open the example workspace.

After that, normal recent-workspace behavior takes over.

This gives the user a complete sigil to inhabit, not a manual to read.
