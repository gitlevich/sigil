---
status: wip
---
# Ontology

Moving through the containment hierarchy. Traversing neighbors, entering contained sigils, ascending to the container. Following an at ('@') reference in the language jumps to that sigil. The full hierarchy is always visible for orientation.
