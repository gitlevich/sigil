# Decision Frame

Viewing: Application > DesignPartner > Memory > Sleep

## User

How about you try to use your memory tools?

## Partner

I can talk about memory from the spec, but I can’t directly operate a separate hidden memory backend unless it is exposed as an actual tool here. In this session, the tools I can really invoke are the editor and spec tools, plus web search.

So if you want, I can do the next best truthful thing: inspect or edit the Memory sigils themselves, or propose the exact tool surface memory would need to become operational rather than just specified.