Each spatial probe takes a whole @sigil as input and produces a number in an interpretable range. Not a judgment, not a suggestion — a number. Out of range = coherence violation. If it can't be computed without author input, it's not a probe — it belongs in @Coverage.

@Coherence itself is more than its probes. The #sense affordance is not numeric — it is the faculty that reads the probes and attends to the shape between them. But the probes themselves are always numbers.
